adapted from https://github.com/Boex9/SatDeck

---

1. 把数据放到文件夹 `./data/ 下`
2. 如果项目根目录有 data.db 文件，删除此文件
3. 在项目根目录，打开终端输入：

```bash
python import_data.py
python app.py
```

4. 浏览器输入 <a href='http:localhost:8000'>http:localhost:8000</a>