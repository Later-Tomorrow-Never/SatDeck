adapted from https://github.com/Boex9/SatDeck

---

# 运行项目

1. 把数据放到文件夹 `./data/ 下`
2. 修改 `./process_data.py` 里的文件路径
3. 如果项目根目录有 data.db 文件，删除此文件
4. 在项目根目录，打开终端输入：

```bash
python import_data.py
python app.py
```

5. 浏览器输入 <a href='http:localhost:8000'>http:localhost:8000</a>